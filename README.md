
  # Interfaz Dashboard Financiero

  Financial management dashboard for SAESTL (Sociedad de Alumnos). A modern, responsive web application for tracking transactions, financial reports, and event planning.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  